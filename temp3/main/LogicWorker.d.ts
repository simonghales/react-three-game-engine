import React from 'react';
import { MappedComponents } from '../shared/types';
declare const LogicWorkerWrapper: React.FC<{
    worker: Worker;
    logicMappedComponents: MappedComponents;
}>;
export default LogicWorkerWrapper;
